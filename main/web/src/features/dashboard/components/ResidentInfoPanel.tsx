// Resident info panel placeholder.
// uses shared/ui/StatCard, shared/ui/TextInput, shared/tokens/spacing
export function ResidentInfoPanel() {
  return <aside aria-label="resident-info-panel-placeholder" />
}
