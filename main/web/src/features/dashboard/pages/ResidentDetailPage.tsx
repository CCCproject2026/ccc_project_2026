// Resident detail page shell.
// uses shared/ui/ResidentInfoPanel, shared/ui/StatCard, shared/tokens/spacing
export function ResidentDetailPage() {
  return <main aria-label="resident-detail-page-placeholder" />
}
