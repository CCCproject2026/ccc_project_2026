// Placeholder AlarmRail component.
// uses shared/tokens/colors, shared/tokens/spacing, shared/tokens/typography
export function AlarmRail() {
	return <section aria-label="alarm-rail-placeholder" />;
}
